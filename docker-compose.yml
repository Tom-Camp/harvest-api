services:
  harvestapi:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    networks:
      - app-network

networks:
  app-network:
    external: true
